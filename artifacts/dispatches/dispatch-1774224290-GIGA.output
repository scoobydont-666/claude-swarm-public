Those three roles (`security`, `sudoers`, `nfs_swarm`) don't exist anywhere on this system — not in the monero-farm repo or anywhere under `/opt`.

The user mentioned they were "tested on miniboss" — they may live on miniboss (192.168.200.213) but were never committed to or synced into this repo on GIGA. A few possibilities:

1. **The roles exist on miniboss** but weren't pushed to GitHub / synced to GIGA yet.
2. **They're in a different repo or directory on miniboss** (e.g., the `ansible-hardening` skill output).
3. **They haven't been written yet** and this is a request to build them.

Before proceeding, I need to know:

- Do these roles exist on miniboss? I can check via SSH if you want.
- Were they generated by the `ansible-hardening` skill and saved somewhere specific?
- Or should I build them now for GIGA using the `ansible-hardening` skill?

Running `ansible-playbook` against a role that doesn't exist will fail immediately, so I won't proceed until we locate or build the roles.
