hostname: orchestration-node
session_id: evening-marathon
timestamp: "2026-03-26T03:20:00Z"
project: hydra-project (ExamForge + Christi + gpu-server-2 + fleet)
duration_minutes: 240
key_decisions:
  - gpu-server-2 (10.0.0.2) onboarded as Docker Swarm worker with Ollama + ComfyUI
  - Ollama GPU fix on gpu-server-1 — Docker container had NVML failure, restart fixed it
  - ExamForge 100 CLF-C02 questions generated on GPU (100% success, ~10s/question)
  - Christi v3 trained (546 Q&A + 1946 knowledge, 3 epochs) but output is degenerate (repetition loop)
  - P2Pool peer list write fix (systemd ReadWritePaths + Ansible role)
  - ClauseHound test mock fix (batch tests no longer hang)
files_changed:
  - examforge: 100 CLF-C02 drafts, timeout/logging improvements, AWS cert onboarded, 650q in DB
  - christi: v3 training data prep + finetune script + GGUF deployed (but quality poor)
  - monero-farm: P2Pool ReadWritePaths fix
  - clausehound: LLM classifier mock in batch tests
  - hydra-project: gpu-server-2 added to fleet table, Hydra Media added to sub-projects
  - swarm config: gpu-server-2 registered with gpu+ollama+comfyui capabilities
issues_found:
  - Christi v3 produces degenerate output (number repetition loop) — mixing pretraining chunks with ChatML Q&A in SFTTrainer may cause format confusion. v1 still best model.
  - gpu-server-1 SSH was down (Josh fixed manually on console)
  - Ollama Docker NVML init failure — container restart fixed GPU access
artifacts_produced:
  - 100 AWS CLF-C02 draft questions (4 YAML files, committed)
  - christi-tax:v3 deployed to Ollama (but degenerate — needs investigation)
  - gpu-server-2 fully operational (Swarm + Ollama + ComfyUI + Claude Code)
  - ExamForge DB now has 650 total questions across 7 certifications
  - QA review running in background on gpu-server-2 Ollama
context_for_next: >
  EXAMFORGE (P0): 650 questions in DB (106 CLF-C02 + CPA sections). QA review running
  via phi4:14b on gpu-server-2 — check qa_results table when complete. Next: review QA results,
  fix flagged questions, run Christi cross-validation on CPA questions.
  CHRISTI: v3 is DEGENERATE (repetition loops). Root cause: likely pretraining chunks
  ({text:...}) mixed with ChatML ({messages:[...]}) confuses SFTTrainer. Fix: either
  (a) convert knowledge chunks to ChatML format (system+user+assistant), or (b) train
  Q&A-only (546 examples, no knowledge chunks), or (c) use two-stage training (pretrain
  first, then SFT). v1 remains best model. RAG+v1 is the production path.
  gpu-server-2: Fully operational. ComfyUI at http://10.0.0.2:8188 (GPU verified).
  Ollama at http://10.0.0.2:11434 (GPU verified). Claude Code installed.
  FLEET: 3 GPU-capable nodes (gpu-server-1 RTX 5080, gpu-server-2 RTX 5060 Ti, rainbow offline).
  2,483 tests across 11 projects, all green.
  BLOCKED: Solar Sentinel (JWT), Hydra Media M5 (OAuth2), Rainbow (physical access).
