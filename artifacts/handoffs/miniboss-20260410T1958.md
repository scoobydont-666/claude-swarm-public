# Session Handoff — miniboss 2026-04-10

## Session Summary
- **Duration:** ~110 minutes (18:09Z — 19:58Z)
- **Primary project:** ExamForge (3 commits, 40 TCP questions generated) + ntnx-sentinel (test rehab)
- **Model:** claude-opus-4-6 (1M context)
- **Trigger:** `/project-management` → `/task-master` autonomous execution

## Completed (with timestamps)

| Time (UTC) | Repo | Commit | What |
|------------|------|--------|------|
| 18:15 | taxprep-project | d0340b5 | Committed dirty uv.lock (alembic dep) |
| 18:25 | ntnx-sentinel | c2eeee0 | **Phase 5 test rehab:** replaced 130 tautological integration tests with 53 real tests. Added `lib.rs` for crate-level access, `tempfile` dev-dep. 132 total tests (79 unit + 53 integration), all passing. |
| 18:30 | — | — | Verified code-consistency skill already has C support (references/c.md, 254 lines). Updated stale memory. |
| 18:35 | monero-farm | bdbbd62 | Committed uv.lock for reproducible builds |
| 18:36 | fast-tools-mcp | dff732b | Committed TEST_SUMMARY.md (317 tests documented) |
| 18:38 | — | — | NTNX Suite Evolution verified ALL 5 phases complete. Memory updated. |
| 18:42 | hydra-project | 81a0cea | Committed FusionIO RE research docs + force_format.c + .gitignore cleanup (excluded data/output/results/uploads/metrics/checkpoints dirs) |
| 18:45 | — | — | Cleaned 10 stale swarm pending tasks (task-003, task-900–914) |
| 18:50 | examforge | e558762 | Added MONGO as Ollama endpoint for generate + validate commands |
| 18:52 | nutantforge | 0bcba9a | Added MONGO endpoint + **fixed MEGA port bug** (11435→11434) |
| 19:05 | — | — | Exposed MONGO Ollama to LAN: `OLLAMA_HOST=0.0.0.0` in systemd override, restarted. Direct access at `192.168.201.85:11434`. |
| 19:10 | examforge | 57eb308 | 20 TCP draft questions (Domain 3 Entity Tax Planning + Domain 4 Property Transactions) via MONGO RTX 5080 qwen3:14b |
| 19:20 | — | — | Created MECHA Ollama override (`/etc/systemd/system/ollama.service.d/override.conf`) with `OLLAMA_HOST=0.0.0.0`, `KEEP_ALIVE=24h`, `MAX_LOADED_MODELS=2`. **Not yet restarted** — qwen3:14b pull in progress. |
| 19:40 | examforge | 5838f9a | 20 more TCP draft questions (Domain 1 Individuals + Domain 2 Entity Tax Compliance) via MONGO qwen3:14b |

**Totals:** 11 commits, 6 repos, 0 failures. All repos clean post-session.

## Failed / Abandoned

| Item | Root Cause | Impact |
|------|-----------|--------|
| ExamForge generation on MECHA | MECHA only had qwen3:8b; ExamForge hardcodes `qwen3:14b`. Ollama tried auto-pull but stalled. | Rerouted all generation to MONGO instead. No lost output. |
| Initial "MECHA unreachable" assessment | Bad shell escape quoting in nested SSH+curl+python3 commands. MECHA was fine the whole time. | ~15 min wasted before user corrected. |
| MECHA qwen3:14b pull via SSH tunnel | Still in progress at session end. 9GB download through SSH tunnel is slow. | Model will be available next session. Just needs `systemctl restart ollama`. |

## Blockers

| Blocker | Owner | Impact |
|---------|-------|--------|
| **GIGA unreachable** (<primary-node-ip>) | Josh (physical/network) | Blocks: NAI Suite Launch, creative LLM eval, heavy inference (96GB GPU) |
| **MEGA unreachable** (<gpu-node-ip>) | Josh (physical/network) | Blocks: dual-5080 workloads |
| NutantForge Phase 4+ | Josh (NAI hardware access) | Blocks POC deployment to Nutanix infra |
| Solar Sentinel Phase 2 | Josh (Enphase JWT credential) | Blocks real API data |
| Christi Phase 30 | Josh (legal review for SaaS) | Blocks public advisory endpoint |

## Active Background Jobs

| Host | Task | Status | Action Needed |
|------|------|--------|---------------|
| MECHA | qwen3:14b pull via SSH tunnel (local:11436→MECHA:11434) | In progress | After pull: `ssh mecha 'sudo systemctl daemon-reload && sudo systemctl restart ollama'` |

No GPU inference jobs running. All ExamForge generation completed and committed.

## Context for Next Session

40 TCP draft questions sit in `/opt/examforge/backend/seed/cpa-tcp-drafts/batch-2026-04-10*.yaml` awaiting review — run `cd /opt/examforge/backend && source .venv/bin/activate && examforge review --cert cpa-tcp` to approve/reject them into the DB. TCP was the weakest cert at 203 questions; these 40 drafts would bring it to ~243 after QA filtering. MONGO is now LAN-accessible at `192.168.201.85:11434` with qwen3:14b loaded — use `--endpoint mongo` directly, no tunnels needed. MECHA's Ollama override is written but needs a restart once the qwen3:14b pull finishes.

## Key Decisions Made

1. **ntnx-sentinel gets a lib.rs** — Binary crate modules were all private (`mod` not `pub mod`), making integration tests impossible. Added `lib.rs` re-exporting all modules as `pub`, refactored `main.rs` to `use ntnx_sentinel::*`. Standard Rust pattern for testable binaries.

2. **MONGO Ollama exposed to LAN** — Was localhost-only. Added `OLLAMA_HOST=0.0.0.0` to systemd override. All fleet Ollama instances should bind 0.0.0.0 per `feedback_ollama_access.md` and `feedback_grafana_lan.md` conventions.

3. **ExamForge+NutantForge now support 4 GPU endpoints** — giga, mecha, mega, mongo. ExamForge uses env vars (`EXAMFORGE_OLLAMA_*`), NutantForge uses hardcoded IPs. Also fixed NutantForge MEGA port bug (was 11435, should be 11434).

4. **TCP question generation strategy** — Hit all 4 domains (10 each) rather than concentrating on one. Domain distribution was uneven (31–76), so broad coverage > deep coverage on the weakest domain.
