# Session Handoff — 2026-04-11

## What Got Done

### Fleet Infrastructure
- **GPU fan control deployed fleet-wide** — new script: 50% default, 100% over 70°C, 65°C cooldown. Deployed to `/usr/local/bin/` on MEGA, MECHA, MONGO (eliminates NFS dependency on GIGA). Committed to ai-project (`7551d6d`)
- **MEGA Ollama bootstrapped** — systemd service, 2x RTX 5080, phi4:14b + qwen3:14b. Fixed port override, UFW rule for fleet agent
- **MECHA Ollama installed natively** — Ollama 0.20.5 systemd (replaces dead K3s pod). Fixed K3s CNI iptables DNAT conflict. phi4:14b pulled
- **MEGA BMC diagnosed** — "Unrecoverable video controller failure" was GNOME sleep, not hardware. Josh disabled display manager. Sleep targets masked
- **Stale NFS mounts cleared** — lazy unmount on MECHA/MONGO, MECHA rebooted to clear kernel hang
- **GitHub token sanitized** — removed hardcoded `gho_` token from MEGA claude-swarm remote
- **Redis protected-mode disabled** on miniboss (celery workers unblocked)
- **Git repos synced** — all repos pulled to origin/main on MEGA, MECHA, MONGO
- **Claude configs deployed** — CLAUDE.md template updated (K3s, GPU specs, MONGO), bmc-ops skill added. All hosts at 62 skills

### Hydra Control Center (Head #18)
- **Phase 6 deployed** — standalone Next.js on miniboss:3300, fleet agents on 4 hosts:8591 (`eb1cc1f`)
- **Dashboard overhauled** — shadcn/ui (Card, Badge, Skeleton, Separator, Table, Dialog, Tooltip, Button), removed demo data fallback, normalized fleet API, multi-GPU aggregation for MEGA, loading skeletons, cluster summary bar, live indicator (`386ae73`)

### ExamForge
- **Security+ SY0-701**: +410 questions generated via MONGO phi4:14b (544→954, target 750 exceeded). Total inventory: 7,883 questions across 13 certs (`44bcada`)

### Tooling
- **fleet-sync-verify.sh** — automated fleet health check: reachability, git sync, fan control, Ollama, fleet agents, dashboard, Claude config. 22 PASS / 0 FAIL (`771bc3d`)
- **bmc-ops skill** — copied from MECHA, deployed fleet-wide via claude-config (`124b887`)

## Decisions Made

1. **Fan control locally, not NFS** — GIGA being down exposed NFS as single point of failure for fan control. Script now lives at `/usr/local/bin/gpu-fan-control.py` on each GPU host
2. **Native Ollama on MECHA** — K3s pod can't restart without GIGA. Installed systemd service as permanent replacement. When GIGA returns, need to either disable Ollama DaemonSet on MECHA or stop systemd service
3. **Dashboard: no fake data ever** — removed all demo/random fallback. Offline hosts show "Unreachable" with clear error state
4. **MEGA video failures = GNOME sleep** — not hardware. Display manager was suspending GPUs. Josh fixed at OS level

## What's Next

1. **GIGA comes back** — restore K3s cluster, decide MECHA DaemonSet vs native Ollama, NFS recovery
2. **Remaining plan items** (deferred):
   - Swarm NFS mounts on MEGA/MONGO
   - Security stack (fail2ban/CrowdSec on MEGA/MECHA)
   - GPU Guardian (9095) deployment
   - Session Snitch v4 deployment
   - Code RAG (8523) — needs new home while GIGA is down
3. **Dashboard Phase 2** — services page + telemetry page redesign, events page (BMC SEL viewer)
4. **Presentations** — 3 decks (2 Nutanix updates + 1 portfolio)
5. **MEGA Ollama upgrade** — still on 0.19.0, should be 0.20.5

## Blockers

- **GIGA offline** — K3s control plane, NFS server, 96GB inference, NAI suite
- **Dashboard services/telemetry pages** — still using old design (only fleet overview was overhauled)

## State Changes

### New Services Deployed This Session
| Service | Host(s) | Port | Status |
|---------|---------|------|--------|
| hydra-control-center | miniboss | 3300 | active (systemd) |
| hydra-fleet-agent | miniboss, MECHA, MEGA, MONGO | 8591 | active (systemd) |
| gpu-fan-control | MEGA, MECHA, MONGO | — | active (systemd) |
| ollama (native) | MECHA | 11434 | active (systemd, replaces K3s pod) |
| ollama (native) | MEGA | 11434 | active (systemd) |

### Fleet GPU State (End of Session)
| Host | GPU | Fan | Temp | Ollama | Models |
|------|-----|-----|------|--------|--------|
| MEGA | 2x RTX 5080 32GB | 50% | 25-26°C | 0.19.0 | phi4:14b, qwen3:14b |
| MECHA | RTX 5060 Ti 16GB | 50% | 24°C | 0.20.5 | phi4:14b |
| MONGO | RTX 5080 16GB | 50% | 27°C | 0.20.2 | phi4:14b, qwen3:14b, nomic-embed-text |
| GIGA | RTX PRO 6000 96GB | — | — | offline | — |

### Verification
`<hydra-project-path>/scripts/fleet-sync-verify.sh` — 22 PASS, 0 FAIL

### Warning for Next Session
- MECHA has native Ollama AND K3s agent configured. When GIGA returns, K3s will try to schedule Ollama pod on MECHA (port 11434 conflict). Options: disable DaemonSet, add taint, or stop systemd ollama
- MEGA's stale NFS mount to `/opt/ai-shared` will try to reconnect when GIGA returns — should be fine but monitor
