# Session Handoff — orchestration-node 2026-04-10T08:18

## Session Summary
- **Duration:** ~90 minutes (14:45–16:18 UTC)
- **Primary project:** context-bridge-mcp-server
- **Model:** Claude Opus 4.6 (1M context)

## Completed (with timestamps)

| Time (UTC) | Item |
|---|---|
| 14:45–15:00 | Parallel evidence gathering: 3 agents — CB repo deep dive, daily optimization reports analysis, external MCP memory research (14 repos, hybrid retrieval patterns, prompt caching alignment) |
| 15:00–15:10 | Synthesized evidence into structured improvement plan at `/home/josh/.claude/plans/lexical-watching-moth.md` |
| 15:10–15:15 | Plan approved. Diagnosed root cause: 0% CB utilization due to write-only cache (no Read-path integration, 40-60% peek error rate from namespace mismatches) |
| 15:15–15:35 | Implemented Phase 1 changes across 8 files: fuzzy alias matching, namespace fallback, adjacency expansion, search snippet improvements (30→64 tokens), BM25 score exposure, utilization telemetry (cb_tokens_served, cb_cache_hits/misses), hot-file auto-ingest |
| 15:35–15:40 | TypeScript compile clean. 161 tests pass (1 pre-existing api.test.ts failure, not ours) |
| 15:40–15:44 | Docker image rebuilt, container restarted. Verified: `/stats` live with new telemetry, search snippets readable, fuzzy matching returns similar aliases + correct namespaces |
| 15:45 | Committed `2a0080c` to context-bridge-mcp-server, pushed to GitHub |
| 15:50 | External research committed to hydra-project: `c9e19b6`, `d7ef10f`, `9b47861`, `17eb3ac` — 4 MCP research docs (44.6KB, 1,088 lines) |

## Failed / Abandoned
- None. All planned Phase 1 work completed successfully.

## Blockers

| Blocker | Requires |
|---|---|
| Miniboss hardware upgrade (MinisForum AI X1 Pro 470) | Josh physical access — must complete before K3s migration |
| CB K3s migration | Depends on orchestration-node upgrade + K3s agent join |
| Phase 3 hybrid search (Ollama embeddings) | Ollama nomic-embed-text on gpu-server-1 — verify model is loaded |

## Active Background Jobs
- **CB server**: Docker container `context-bridge-mcp-server-context-bridge-1` on orchestration-node, port 8520, healthy. 525 entries, 166K tokens, 8.3% utilization.
- No GPU jobs or background processes started this session.

## Context for Next Session

Context Bridge Phase 1 is complete and deployed (commit `2a0080c`). The improvement plan at `/home/josh/.claude/plans/lexical-watching-moth.md` has Phases 2-4 remaining: search result grouping, hot-file registry expansion (Phase 2), hybrid FTS5+embedding search via Ollama (Phase 3), Grafana dashboard + stale doc cleanup (Phase 4). After orchestration-node hardware upgrade, join orchestration-node to K3s cluster and migrate CB from Docker Compose to K3s Deployment — plan saved at `memory/project_cb_k3s_migration.md`. The pre-existing api.test.ts UNIQUE constraint failure should also be fixed (namespace test, not blocking).

## Key Decisions Made

1. **Root cause of 0% CB utilization**: Write-only cache — nothing routes file reads through CB. Peek errors (40-60%) caused by namespace mismatch (content in `reference` namespace, queries defaulting to `default`).
2. **Phase 1 over v2 rewrite**: Old CRITICAL_PATH.md roadmap targeting auth/CORS/12-week v2 rewrite was wrong priority. Utilization is the real problem. Auth already works. v2 schema rewrite deferred.
3. **No JWT auth**: Single-user LAN system — bearer token sufficient. JWT with scopes is over-engineering.
4. **No knowledge graph**: CB stores documents, not entities. KG layer would be a complete redesign for marginal benefit.
5. **K3s migration after hardware upgrade**: orchestration-node joins K3s as agent, CB migrates to K3s Deployment with nodeSelector. Taint orchestration-node to repel GPU workloads.
6. **Docker Compose for now is pragmatic**: CB is orchestration-node-local with no GPU needs. Docker Compose works until K3s migration.
