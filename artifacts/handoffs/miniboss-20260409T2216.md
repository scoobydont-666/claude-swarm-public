# Session Handoff — orchestration-node 2026-04-09T22:16

## Session Summary
- **Duration:** ~1.75 hours (20:30 – 22:16 PDT)
- **Primary project:** ComfyConfig + ComfyForge (image/video generation stack)
- **Model:** Claude Opus 4.6 (1M context)

---

## Completed

### Grill: Gallery Persistence (~20:35–20:50)
- Standard grill on gallery persistence wiring — 5 decisions resolved:
  1. Save triggered by orchestrator background poller, not frontend
  2. 2-second asyncio poll loop in orchestrator
  3. Idempotency via `generation_id` unique constraint + upsert
  4. Don't evict from `_jobs` until gallery save succeeds
  5. One gallery entry per image, not per batch

### Grill: 10 Uncensored Workflow Plan (~20:50–21:05)
- Proposed 10-workflow target list, verified against fleet inventory
- Swapped Flux + ControlNet → Flux Uncensored HQ (no Flux ControlNet on disk)
- All decisions resolved, no deferrals

### Fleet-Wide ComfyUI Inventory (~21:05–21:25)
- `c460ebb` — 4-node parallel SSH inventory: gpu-server-1, gpu-server-2, gpu-server-4 inventoried, gpu-server-3 confirmed offline
- 1.2TB shared NFS models, 83 custom nodes, all on gpu-server-1 NFS export
- Documented: checkpoints (758G), LoRAs (17G), ControlNet (37G), VAE (3.6G), text encoders (31G)

### Existing Workflow Testing (~21:25–21:35)
- Tested 6 of 7 curated workflows on gpu-server-1 API (upscale skipped — needs input image)
- Results: 4 pass (sdxl-txt2img, sdxl-turbo-fast, juggernaut-portrait, pony-v6-character), 2 fail (flux-schnell-fast, flux-dev-txt2img)
- Root cause: `CheckpointLoaderSimple` returns None CLIP for FP8 Flux checkpoints

### Flux Bug Fix (~21:35–21:45)
- Created non-destructive copies: `flux-schnell-fast-claude.json`, `flux-dev-txt2img-claude.json`
- Fix: replaced `CheckpointLoaderSimple` with `UNETLoader` + `DualCLIPLoader` + `VAELoader`
- Both E2E validated: Schnell 2s, Dev 19s on gpu-server-1

### LoRA Downloads (~21:25–21:35, parallel)
- Downloaded `detail_tweaker_xl.safetensors` (218M) and `pony_enhancement.safetensors` (218M)
- Destination: `/var/lib/ai/comfy-basedir/models/loras/` on gpu-server-1 NFS

### Wan 2.2 Model Symlinks (~21:50–21:55)
- Symlinked 4 Wan 2.2 14B models from `checkpoints/` to `diffusion_models/` (WanVideoModelLoader scans diffusion_models, not checkpoints)
- All 4 now visible in ComfyUI API object_info

### 10 New Workflows Created & Validated (~21:35–22:05)
- `66c35f9` — 10 workflow JSONs committed and pushed to comfyconfig repo
- E2E results:
  - **flux-uncensored.json** — PASS (15s, LoRA loaded)
  - **pony-v6-nsfw.json** — PASS (11s, score tags + enhancement LoRA)
  - **cyberrealistic-pony.json** — PASS (15s)
  - **juggernaut-cinematic.json** — PASS (10s, detail tweaker LoRA)
  - **flux-uncensored-hq.json** — PASS (25s, 30 steps 1024x1024)
  - **wan22-t2v.json** — QUEUED SUCCESSFULLY (no node errors, video rendering)
  - **wan22-i2v.json** — STRUCTURE VALID (needs input image)
  - **sdxl-inpaint.json** — STRUCTURE VALID (needs input image + mask)

### Wan Video Node Schema Resolution (~21:45–22:05)
- First submission had 6 node parameter mismatches — all fixed by querying `/object_info` API
- Key fixes: `model` → `model_name`, `prompt` → `positive_prompt`, `offload_txt_in` → `offload_txt_emb`, added `scheduler` + `riflex_freq_index`, fixed tile params, moved `block_swap_args` from sampler to model loader

---

## Failed / Abandoned

| Item | Root Cause | Resolution |
|------|-----------|------------|
| Hydra Forge + NTNX Sentinel deep grill | Josh said "that's wrong" — incorrect plan item from previous session | Dropped entirely. Awaiting correct replacement if any. |
| gpu-server-1 SSH | Port 22 refused (sshd down), ping OK, all services running | Used gpu-server-2 SSH for NFS access, gpu-server-1 ComfyUI API for testing. Needs sshd restart. |
| Wan 2.2 14B not visible in ComfyUI | Models in `checkpoints/` but WanVideoModelLoader scans `diffusion_models/` | Fixed with symlinks |

---

## Blockers

| Blocker | Owner | Notes |
|---------|-------|-------|
| gpu-server-1 SSH down (port 22 refused) | Josh | Ping OK, all services running. Needs `systemctl restart sshd` or physical console. |
| gpu-server-3 offline since Apr 8 | Josh | No route to host. Physical/network intervention needed. |
| gpu-server-4 K3s GPU operator | Deferred | Docker-based containerd incompatible. gpu-server-4 GPU works via Docker-direct. |
| Original Flux workflows still broken | Next session | `flux-schnell-fast.json` and `flux-dev-txt2img.json` need the same UNETLoader fix applied to originals (not just -claude copies) |

---

## Active Background Jobs

| Service | Host | Port | Status |
|---------|------|------|--------|
| comfyforge.service | orchestration-node | 8508 | active (systemd) |
| comfyconfig-backend.service | orchestration-node | 8592 | active (systemd) |
| comfyconfig-frontend.service | orchestration-node | 3400 | active (systemd) |
| comfyconfig-db (PostgreSQL) | orchestration-node | 5435 | running (Docker) |
| ComfyUI K3s pod | gpu-server-1 | 8188 | Running (hostNetwork) |
| ComfyUI K3s pod | gpu-server-2 | 30188 | Running (NodePort) |
| ComfyUI Docker | gpu-server-4 | 8188 | Running (Docker) |
| Wan 2.2 T2V job | gpu-server-1 | — | Queued (prompt_id: 6b744ec5-8301-4bb6-b136-ac8349c72619) |

---

## Context for Next Session

Pick up from 3 remaining tasks: (1) **Wire gallery persistence** — `GalleryService` at `/opt/comfyconfig/backend/services/gallery_service.py` is fully implemented, `JobManager._jobs` (not Pipeline) at `/opt/comfyforge/src/comfyforge/jobs.py:31` is in-memory dict. Orchestrator at `/opt/comfyconfig/backend/pipeline/orchestrator.py` needs a background asyncio poller + GalleryService injection + idempotency guard. All 5 design decisions resolved in grill. (2) **Full DoD review** for ComfyConfig/ComfyForge — scope complete check, no stubs/fakes, e2e, security, observability. (3) **Evaluate RedPlanet Core** (`github.com/RedPlanetHQ/core`) for Hydra usefulness — memory pointer at `project_redplanet_core_eval.md`.

Also: fix gpu-server-1 SSH (sshd restart), apply Flux UNETLoader fix to original workflow files (not just -claude copies), and check if Wan 2.2 T2V video rendered successfully.

---

## Key Decisions Made

1. **Gallery persistence via orchestrator background poller** — 2-second asyncio loop, not frontend-triggered. Idempotency via generation_id unique constraint.
2. **Flux FP8 workflows require 3-loader pattern** — UNETLoader + DualCLIPLoader + VAELoader instead of CheckpointLoaderSimple. Original files preserved, fixes in -claude copies.
3. **Wan 2.2 14B models symlinked** — `diffusion_models/` is the scan directory for WanVideoModelLoader, not `checkpoints/`. Symlinks are non-destructive.
4. **No Flux ControlNet on fleet** — swapped workflow #8 from Flux+ControlNet to Flux Uncensored HQ (higher value, models available).
5. **CyberRealistic is actually cyberrealisticPony_v65** — a Pony-based model, uses score tags in prompts.
6. **Hydra Forge + NTNX Sentinel plan dropped** — Josh flagged it as incorrect. No replacement specified yet.
7. **Video workflows gpu-server-1-only** — Wan 2.2 14B needs 48GB+ VRAM with block swap. Only gpu-server-1 (96GB) can run them.

---

## Commits This Session

### comfyconfig
```
66c35f9 feat: add 10 uncensored multi-GPU workflows + fix broken Flux pipelines
```

### hydra-project
```
c460ebb docs: comprehensive ComfyUI component inventory — 1.2TB models, 87 custom nodes, gpu-server-1 GPU headroom analysis
```

---

## Checkpoint
`/var/lib/swarm/artifacts/checkpoints/orchestration-node-20260409T2215.yaml`

---

## Memory Files — No Changes This Session
No new memory files created or updated. All grill decisions captured in checkpoint YAML and this handoff.
