# Session Handoff — 2026-04-11

## What Got Done

- **MEGA Ollama bootstrap** — systemd service, 2x RTX 5080 (31.8 GiB), phi4:14b + qwen3:14b pulled. Removed stale port override. MEGA went offline after setup.
- **Claude config fleet sync** — Updated CLAUDE.md template (K3s, GPU specs, MONGO). Deployed to MEGA/MECHA/miniboss with host-specific personas (`a81da0d`)
- **Hydra Control Center Phase 6 DEPLOYED** — standalone Next.js on miniboss:3300, fleet agents on miniboss/MECHA/MONGO:8591. 24 tests pass (`eb1cc1f`)
- **ExamForge Security+ batch gen** — +410 questions via MONGO phi4:14b (544→954, target 750 exceeded). Script: `generate_securityplus.py` (`44bcada`)
- **MECHA Ollama native install** — Ollama 0.20.5 systemd service (replaces dead K3s pod). Fixed K3s iptables DNAT conflict on port 11434. phi4:14b pulled.
- **Redis fix** — protected-mode disabled on miniboss, celery workers unblocked
- **Service version audit** — all MCP servers at HEAD, repos synced, hydra-pulse cleaned
- **Master plan updated** — fleet bootstrap + HCC Phase 6 entries added (`42a0ecd`)

## Decisions Made

1. **Native Ollama on MECHA** — K3s pod can't restart without GIGA. Installed Ollama systemd service as permanent replacement. When GIGA returns, K3s Ollama DaemonSet will conflict — need to either disable the DaemonSet on MECHA or stop the systemd service.
2. **Removed K3s iptables rule on MECHA** — stale CNI-HOSTPORT-DNAT was redirecting port 11434 to dead pod IP. Will regenerate when K3s reconciles on GIGA return.
3. **MONGO for inference** — MECHA was hung, MONGO phi4:14b responded instantly. MONGO is a reliable inference node.
4. **Ran services as josh** — aisvc user created but venv ownership is josh. Simplest path for now.

## What's Next (Priority Order)

1. **GIGA comes back** → restore K3s cluster, decide on MECHA DaemonSet vs systemd Ollama
2. **MEGA fleet agent** — deploy when MEGA comes back online
3. **NAI suite launch** — 7 services, needs GIGA K3s
4. **Presentations** — 3 decks (2 Nutanix updates + 1 portfolio). Need live screenshots.
5. **Code-consistency skill refresh** — add C language support
6. **Fix ratio reduction** — 12.5% → <8% target

## Blockers

- **GIGA offline** — K3s control plane, NAI suite, 96GB inference all unavailable
- **MEGA intermittent** — configured but keeps dropping off network
- **Presentations** — need Josh to specify screenshot targets and content priorities

## State Changes

### New Services Running
| Service | Host | Port | Type |
|---------|------|------|------|
| hydra-control-center | miniboss | 3300 | Next.js dashboard (systemd) |
| hydra-fleet-agent | miniboss | 8591 | FastAPI (systemd) |
| hydra-fleet-agent | MECHA | 8591 | FastAPI (systemd) |
| hydra-fleet-agent | MONGO | 8591 | FastAPI (systemd) |
| ollama | MEGA | 11434 | Inference (systemd, offline) |
| ollama | MECHA | 11434 | Inference (systemd, replaces K3s pod) |

### Fleet GPU State
| Host | Status | GPU | Ollama Models |
|------|--------|-----|---------------|
| MONGO | online | RTX 5080 16GB | phi4:14b, qwen3:14b, nomic-embed-text |
| MECHA | online | RTX 5060 Ti 16GB | phi4:14b |
| MEGA | offline | 2x RTX 5080 32GB | phi4:14b, qwen3:14b (configured) |
| GIGA | offline | RTX PRO 6000 96GB | — |

### Data Changes
- ExamForge: 7,883 total questions (was 7,473). Security+ 954 (was 544).
- Redis: protected-mode off on miniboss
- K3s iptables: CNI-HOSTPORT-DNAT rule removed on MECHA for port 11434

### Warning for Next Session
When GIGA returns and K3s reconciles, the Ollama DaemonSet will try to schedule a pod on MECHA. This will conflict with the native systemd Ollama on port 11434. Options:
1. Delete the MECHA systemd ollama service and let K3s manage it
2. Add a nodeSelector/taint to exclude MECHA from the DaemonSet
3. Change the systemd Ollama to a different port
