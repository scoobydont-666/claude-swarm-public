# Session Handoff — 2026-04-11

## What Got Done

- **gpu-server-3 Ollama bootstrap** — systemd service, 2x RTX 5080 (31.8 GiB), phi4:14b + qwen3:14b pulled. Removed stale port override. gpu-server-3 went offline after setup.
- **Claude config fleet sync** — Updated CLAUDE.md template (K3s, GPU specs, gpu-server-4). Deployed to gpu-server-3/gpu-server-2/orchestration-node with host-specific personas (`a81da0d`)
- **Hydra Control Center Phase 6 DEPLOYED** — standalone Next.js on orchestration-node:3300, fleet agents on orchestration-node/gpu-server-2/gpu-server-4:8591. 24 tests pass (`eb1cc1f`)
- **ExamForge Security+ batch gen** — +410 questions via gpu-server-4 phi4:14b (544→954, target 750 exceeded). Script: `generate_securityplus.py` (`44bcada`)
- **gpu-server-2 Ollama native install** — Ollama 0.20.5 systemd service (replaces dead K3s pod). Fixed K3s iptables DNAT conflict on port 11434. phi4:14b pulled.
- **Redis fix** — protected-mode disabled on orchestration-node, celery workers unblocked
- **Service version audit** — all MCP servers at HEAD, repos synced, hydra-pulse cleaned
- **Master plan updated** — fleet bootstrap + HCC Phase 6 entries added (`42a0ecd`)

## Decisions Made

1. **Native Ollama on gpu-server-2** — K3s pod can't restart without gpu-server-1. Installed Ollama systemd service as permanent replacement. When gpu-server-1 returns, K3s Ollama DaemonSet will conflict — need to either disable the DaemonSet on gpu-server-2 or stop the systemd service.
2. **Removed K3s iptables rule on gpu-server-2** — stale CNI-HOSTPORT-DNAT was redirecting port 11434 to dead pod IP. Will regenerate when K3s reconciles on gpu-server-1 return.
3. **gpu-server-4 for inference** — gpu-server-2 was hung, gpu-server-4 phi4:14b responded instantly. gpu-server-4 is a reliable inference node.
4. **Ran services as josh** — aisvc user created but venv ownership is josh. Simplest path for now.

## What's Next (Priority Order)

1. **gpu-server-1 comes back** → restore K3s cluster, decide on gpu-server-2 DaemonSet vs systemd Ollama
2. **gpu-server-3 fleet agent** — deploy when gpu-server-3 comes back online
3. **NAI suite launch** — 7 services, needs gpu-server-1 K3s
4. **Presentations** — 3 decks (2 Nutanix updates + 1 portfolio). Need live screenshots.
5. **Code-consistency skill refresh** — add C language support
6. **Fix ratio reduction** — 12.5% → <8% target

## Blockers

- **gpu-server-1 offline** — K3s control plane, NAI suite, 96GB inference all unavailable
- **gpu-server-3 intermittent** — configured but keeps dropping off network
- **Presentations** — need Josh to specify screenshot targets and content priorities

## State Changes

### New Services Running
| Service | Host | Port | Type |
|---------|------|------|------|
| hydra-control-center | orchestration-node | 3300 | Next.js dashboard (systemd) |
| hydra-fleet-agent | orchestration-node | 8591 | FastAPI (systemd) |
| hydra-fleet-agent | gpu-server-2 | 8591 | FastAPI (systemd) |
| hydra-fleet-agent | gpu-server-4 | 8591 | FastAPI (systemd) |
| ollama | gpu-server-3 | 11434 | Inference (systemd, offline) |
| ollama | gpu-server-2 | 11434 | Inference (systemd, replaces K3s pod) |

### Fleet GPU State
| Host | Status | GPU | Ollama Models |
|------|--------|-----|---------------|
| gpu-server-4 | online | RTX 5080 16GB | phi4:14b, qwen3:14b, nomic-embed-text |
| gpu-server-2 | online | RTX 5060 Ti 16GB | phi4:14b |
| gpu-server-3 | offline | 2x RTX 5080 32GB | phi4:14b, qwen3:14b (configured) |
| gpu-server-1 | offline | RTX PRO 6000 96GB | — |

### Data Changes
- ExamForge: 7,883 total questions (was 7,473). Security+ 954 (was 544).
- Redis: protected-mode off on orchestration-node
- K3s iptables: CNI-HOSTPORT-DNAT rule removed on gpu-server-2 for port 11434

### Warning for Next Session
When gpu-server-1 returns and K3s reconciles, the Ollama DaemonSet will try to schedule a pod on gpu-server-2. This will conflict with the native systemd Ollama on port 11434. Options:
1. Delete the gpu-server-2 systemd ollama service and let K3s manage it
2. Add a nodeSelector/taint to exclude gpu-server-2 from the DaemonSet
3. Change the systemd Ollama to a different port
