# Session Handoff — 2026-04-10

## What Got Done

### Session Snitch v4 "Fleet Intelligence"
- **SQL injection hardening** (fdf78cd) — Replaced all `format!()` SQL interpolation with parameterized `?1` queries in main.rs and otel.rs. Deleted `period_filter()`. Zero interpolated SQL remains.
- **Skills CLI command** (fdf78cd) — `hydra-pulse skills [--period]` shows skill activation frequency with invocation count, session count, and bar chart. Also added `GET /api/skills` web endpoint.
- **Auto-calibration** (fdf78cd) — `auto_calibrate_from_snapshot()` reads fresh statusline snapshots and auto-sets calibration keys. Eliminates manual `calibrate --weekly-pct X --burst-pct Y` workflow.
- **Predictive forecast** (fdf78cd) — `hydra-pulse forecast` computes token velocity from snapshots, extrapolates to window end, shows when 80%/100% thresholds will be hit. Also added `GET /api/forecast`.
- **Dead code annotated** (fdf78cd) — `estimate_savings` and `recommend_session_model` tagged `#[allow(dead_code)]` with TODO for v3.2.

### DoD Completion
- **87 tests** (0e109bd) — 39 unit + 36 integration + 12 web API (was 66). Added tests for auto-calibration, budget projections, skills, forecast, and all 12 web API endpoints.
- **README + skill updated** — Phase 4 documented, all CLI commands listed, test inventory current.

### Fleet Deployment
- **MECHA** — Updated from Mar 26 build to v4. Doctor all green (17 sessions tracked).
- **MONGO** — Fresh install. Fixed root-owned ~/.config and ~/.local. Doctor all green.
- **miniboss** — Primary host, already running v4.

### DoD Skill
- Created `~/.claude/skills/definition-of-done/SKILL.md` — 10-point mandatory checklist with evidence requirements.

## Decisions Made
- Deferred full rename (hydra-pulse → session-snitch binaries) — high churn, no functional value
- Deferred cross-host DB aggregation (2.4) — needs NFS vs PostgreSQL architectural decision
- Deferred smart model routing (3.2) — kept dead code annotated for future use
- Used reqwest blocking client for web tests — tests real HTTP stack, simpler than axum test utilities

## What's Next
1. **Install session-snitch on GIGA and MEGA** when they come back online (same SCP + doctor + timer pattern)
2. **v2.0 rename session** — Rename binaries from hydra-pulse to session-snitch, update hooks, symlinks for transition
3. **Cross-host DB aggregation** (2.4) — Design session needed: NFS shared DB vs PostgreSQL vs periodic DB merge
4. **Smart model routing** (3.2) — Analyze historical sessions to recommend cheaper models, wire into token-miser hook
5. **OTel test coverage** — otel.rs still has 0 tests (359 LOC)

## Blockers
- **GIGA unreachable** — Cannot install session-snitch v4 or verify fleet-wide
- **MEGA unreachable** — Same

## State Changes
- Session Snitch v4 deployed on 3/5 fleet hosts (miniboss, MECHA, MONGO)
- MONGO: fixed root-owned ~/.config and ~/.local directories
- New skill: definition-of-done (10-point DoD checklist)
- hydra-pulse skill file updated with v4 commands and 87-test count
- Test count: 66 → 87 across 3 test files
