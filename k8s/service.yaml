apiVersion: v1
kind: Service
metadata:
  name: claude-swarm
  labels:
    app.kubernetes.io/name: claude-swarm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: claude-swarm
  ports:
    - port: 9192
      targetPort: 9192
      protocol: TCP
      name: http
