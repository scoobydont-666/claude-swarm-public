#!/usr/bin/env python3
"""hydra-ipc — agent-to-agent IPC for Claude Code instances."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))

from ipc.cli import main

main()
